diff --git a/src/xrGame/ui/UIActorMenuInventory.cpp b/src/xrGame/ui/UIActorMenuInventory.cpp
index 8dd1db6e3..000000000 100644
--- a/src/xrGame/ui/UIActorMenuInventory.cpp
+++ b/src/xrGame/ui/UIActorMenuInventory.cpp
@@ -223,6 +223,17 @@ bool FindItemInList(CUIDragDropListEx* lst, PIItem pItem, CUICellItem*& ci_res)
 	return false;
 }
 
+// GCS performance: callers that already paid the cost of FindItemInList should
+// not scan the same drag/drop list a second time just to remove the found cell.
+bool RemoveFoundItemFromList(CUIDragDropListEx* lst, CUICellItem* ci)
+{
+	if (!ci)
+		return false;
+
+	CUICellItem* dying_cell = lst->RemoveItem(ci, false);
+	xr_delete(dying_cell);
+	return true;
+}
+
 bool RemoveItemFromList(CUIDragDropListEx* lst, PIItem pItem)
 {
 	// fixme
@@ -230,11 +241,7 @@ bool RemoveItemFromList(CUIDragDropListEx* lst, PIItem pItem)
 	if (FindItemInList(lst, pItem, ci))
 	{
 		R_ASSERT(ci);
-
-		CUICellItem* dying_cell = lst->RemoveItem(ci, false);
-		xr_delete(dying_cell);
-
-		return true;
+		return RemoveFoundItemFromList(lst, ci);
 	}
 	else
 		return false;
@@ -287,7 +294,7 @@ void CUIActorMenu::OnInventoryAction(PIItem pItem, u16 action_type)
 				if (FindItemInList(curr, pItem, ci))
 				{
 					if (lst_to_add != curr)
-						RemoveItemFromList(curr, pItem);
+						RemoveFoundItemFromList(curr, ci);
 					else
 						b_already = true;
 					//break;
@@ -303,7 +310,7 @@ void CUIActorMenu::OnInventoryAction(PIItem pItem, u16 action_type)
 					if (FindItemInList(curr, pItem, ci))
 					{
 						if (lst_to_add != curr)
-							RemoveItemFromList(curr, pItem);
+							RemoveFoundItemFromList(curr, ci);
 						else
 							b_already = true;
 					}
@@ -523,6 +530,8 @@ void CUIActorMenu::FilterActorBagList(int mode)
 	ruck_list = m_pActorInvOwner->inventory().m_ruck;
 	std::sort(ruck_list.begin(), ruck_list.end(), InventoryUtilities::GreaterRoomInRuck);
 
+	const bool show_all = (0 == xr_strcmp(m_sort_kinds[mode], "s_all"));
+	const int kinds = show_all ? 0 : _GetItemCount(m_sort_kinds[mode]);
 	TIItemContainer::iterator itb = ruck_list.begin();
 	TIItemContainer::iterator ite = ruck_list.end();
 	for (; itb != ite; ++itb)
@@ -534,9 +543,7 @@ void CUIActorMenu::FilterActorBagList(int mode)
 			continue;
 
 		PIItem iitm = *itb;
-		int kinds = _GetItemCount(m_sort_kinds[mode]);
-
-		if (0 == xr_strcmp(m_sort_kinds[mode], "s_all"))
+		if (show_all)
 		{
 			CUICellItem* itm = create_cell_item(iitm);
 			m_pInventoryBagList->SetItem(itm);
diff --git a/src/xrGame/ui/UIActorMenuTrade.cpp b/src/xrGame/ui/UIActorMenuTrade.cpp
index f7652b276..000000000 100644
--- a/src/xrGame/ui/UIActorMenuTrade.cpp
+++ b/src/xrGame/ui/UIActorMenuTrade.cpp
@@ -91,6 +91,8 @@ void CUIActorMenu::FilterActorTradeBagList(int mode)
 	ruck_list = m_pActorInvOwner->inventory().m_ruck;
 	std::sort(ruck_list.begin(), ruck_list.end(), InventoryUtilities::GreaterRoomInRuck);
 
+	const bool show_all = (0 == xr_strcmp(m_sort_kinds[mode], "s_all"));
+	const int kinds = show_all ? 0 : _GetItemCount(m_sort_kinds[mode]);
 	TIItemContainer::iterator itb = ruck_list.begin();
 	TIItemContainer::iterator ite = ruck_list.end();
 	for (; itb != ite; ++itb)
@@ -105,9 +107,7 @@ void CUIActorMenu::FilterActorTradeBagList(int mode)
 
 			PIItem iitm = *itb;
 
-			int kinds = _GetItemCount(m_sort_kinds[mode]);
-
-			if (0 == xr_strcmp(m_sort_kinds[mode], "s_all"))
+			if (show_all)
 			{
 				CUICellItem* itm = create_cell_item(iitm);
 				m_pTradeActorBagList->SetItem(itm);
@@ -151,6 +151,8 @@ void CUIActorMenu::FilterTraderList(int mode)
 	m_pPartnerInvOwner->inventory().AddAvailableItems(items_list, true);
 	std::sort(items_list.begin(), items_list.end(), InventoryUtilities::GreaterRoomInRuck);
 
+	const bool show_all = (0 == xr_strcmp(m_sort_kinds[mode], "s_all"));
+	const int kinds = show_all ? 0 : _GetItemCount(m_sort_kinds[mode]);
 	TIItemContainer::iterator itb = items_list.begin();
 	TIItemContainer::iterator ite = items_list.end();
 	for (; itb != ite; ++itb)
@@ -159,9 +161,7 @@ void CUIActorMenu::FilterTraderList(int mode)
 		if (!is_item_in_list(m_pTradePartnerList, *itb))
 		{
 			PIItem iitm = *itb;
-			int kinds = _GetItemCount(m_sort_kinds[mode]);
-
-			if (0 == xr_strcmp(m_sort_kinds[mode], "s_all"))
+			if (show_all)
 			{
 				CUICellItem* itm = create_cell_item(*itb);
 				m_pTradePartnerBagList->SetItem(itm);
